(library
 (name figma_mcp)
 (public_name figma_mcp)
 (libraries
   mcp_protocol
   mcp_protocol_eio
   yojson
   uri
   str
   unix
   ipaddr
   base64
   ;; Eio (Pure: HTTP server + gRPC server + Figma API client)
   eio
   eio_main
   eio.unix
   httpun-eio
   gluten-eio
   cohttp-eio
   tls-eio
   ca-certs
   bigstringaf
   ;; Crypto RNG (for HTTPS/TLS)
   mirage-crypto-rng.unix
   ;; Version from dune-project (auto-sync)
   dune-build-info)
 (wrapped false)
 (ocamlopt_flags (:standard))
 (instrumentation (backend bisect_ppx)))
